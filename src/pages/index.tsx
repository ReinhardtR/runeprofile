import { SearchModal } from "@/components/SearchModal";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <span>Home</span>
    </div>
  );
};

export default Home;
